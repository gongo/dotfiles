# -*-sh-*-

if [ -f $DOTFILES_DIR/dot.zshenv.$(uname -s) ] ; then
    source $DOTFILES_DIR/dot.zshenv.$(uname -s)
fi

if [ -f $HOME/.zshenv.private ] ; then
    source $HOME/.zshenv.private
fi

source $DOTFILES_DIR/rbenv/env
source $DOTFILES_DIR/bundlizer/env
